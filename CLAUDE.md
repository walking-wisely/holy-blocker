# AGENTS.md

Guidance for coding agents working in this repository.

## Project Shape

Holy Blocker is an on-device content blocking project. Keep the privacy and local-first model central when making changes: do not add cloud calls, telemetry, remote content analysis, or external dataset dependencies unless the user explicitly asks for them.

## Current State

The packages below **exist in the repo today** and are actively being built:

| Package | Language | Status |
|---|---|---|
| `apps/desktop` | TypeScript / Electron + React | Skeleton — BrowserWindow, one IPC stub, status UI |
| `packages/text-policy` | Rust | normalize + lexicon + verdict + scorer + evaluator + policy done; FFI surface done (see `packages/text-policy-ffi`) |
| `packages/text-policy-ffi` | Rust | UniFFI wrapper over text-policy — PolicyEngine + evaluate exposed; Kotlin bindings generated for Android, and Swift bindings for the mac daemon (`native-modules/mac-daemon/scripts/build-ffi.sh`). Two consumers now, so **the crate's API is a cross-platform contract** — changing `Action`, `SourceKind` or `Verdict` breaks both, and the score is a `u32` 0–100 that each caller normalizes itself. No Rust changes were needed for the Swift path |
| `apps/mobile` | Kotlin / Android | AccessibilityService text path MVP — policy core + ScanGate + overlay + onboarding done; text-policy wired in over UniFFI; SettingsGuard blocks the accessibility/App Info screens that would remove the guard, but **only while the user has armed the in-app protection mode** — `ProtectionSchedule`/`ProtectionStore` hold that state, and disarming is request → 15-min cooldown → confirm → 10-min window that re-arms itself; device admin active, so uninstall is refused until it is deactivated; all verified on an android-36 arm64 emulator. Plain Device Admin only — never design around Device Owner. The empty-harvest gap is closed: the device-admin list rows are `accessibilityDataSensitive` and needed `android:isAccessibilityTool="true"`, without which the guard is handed chrome and no rows — see `docs/components/mobile/backlog.md`. Split screen is handled: every watched window is evaluated, and `GLOBAL_ACTION_BACK` — which takes no window argument — is gated on the matched window holding input focus, covering instead when it does not; verified in real split screen on the emulator, and note an unfocused pane emits no accessibility events at all. The tamper log is done — `policy/TamperLog.kt` (pure) + `TamperLogStore.kt` (the `filesDir` edge) record guard-state transitions and removal attempts, and classify a session that ended without an unbind as an unclean stop; verified on an android-36 emulator against a force-stop, a clean off/on, and a fresh install. It records the guard, never the screen. Recents is **deferred, blocked on real One UI/HyperOS hardware** — the swipe-kill claim cannot be reproduced on an emulator, so no mitigation can be verified. The foreground service is done — `policy/GuardStatus.kt` (pure) + `GuardStatusService.kt` + `BootReceiver.kt`: an ongoing `specialUse` FGS that is the status surface **and** the still-alive process that records a disable the guard cannot report (`adb`, guest, an OEM disable screen we do not recognise), closing the other half of the backlog's undetectable-bypass item. It does **not** keep the guard alive — the system rebinds an enabled accessibility service by itself. `TamperLog.classifyConnect` now takes a tail of entries and anchors on the last session boundary, because the status service writes between sessions and a single-line read turned every clean off/on into an apparent kill. `BootReceiver` writes nothing: `ACTION_BOOT_COMPLETED` is delivered when the app leaves the force-stopped state, so a boot receiver must never be what identifies a boot. Verified on an android-36 emulator against an adb disable, a reboot, a force-stop, and an idle install. The VPN path is done and **verified on an android-36 arm64 emulator** via `scripts/smoke-test-vpn.sh` — `policy/NetworkGuard.kt` (pure) + `NetworkGuardService.kt` + `policy/Blocklist.kt`/`BlocklistStore.kt` (rules from `filesDir/blocklist.txt`) over `packages/net-shield-ffi`: a `VpnService` that answers blocked names with NXDOMAIN locally and forwards everything else to the underlying network's resolvers over a `protect()`ed socket. **It filters DNS only, and the TUN claims exactly one /32 route so nothing else can reach it** — this is forced by the platform, not a staging choice: an Android TUN cannot re-inject a packet the way Wintun can, so permitting a TCP flow needs a userspace TCP stack, and widening the route before that exists black-holes the device rather than filtering it. SNI/IP filtering is therefore a later step. Two traps recorded in the plan: once the VPN is up **our own TUN address is the system's advertised resolver**, so any "read the current DNS servers" path loops packets back into the TUN (guarded twice — `NET_CAPABILITY_NOT_VPN` on the `NetworkRequest`, and `NetworkGuard.upstreamResolvers` dropping our addresses); and NXDOMAIN is used rather than a sink address because one refusal covers A, AAAA and HTTPS/SVCB at once. There is deliberately **no fallback to a public resolver**. The VPN settings screen is **not** in `SettingsProfiles` — §7's rule is that every identifier is dumped from a device, never inferred — so revocation is recorded (`NETWORK_GUARD_REVOKED`) rather than blocked for now. Three defects the unit tests could not catch, all found on the first emulator run and all recorded in the plan: `ACCESS_NETWORK_STATE` was missing, so `registerNetworkCallback` threw *after* the TUN was established; `INTERNET` was missing, and **that failure looks exactly like the filter working** — blocked names refused, permitted ones silently unanswered — so always assert both directions; and **Android does not re-establish a `VpnService` after a reboot** (`setAlwaysOnVpnPackage` is owner-only), so `BootReceiver` restores it. Two smoke-test traps: netd answers from cache without asking the resolver and `ndc resolver flushnetdns` fails silently for a non-root shell, so the script reboots to get a cold cache; and assert on `ping`'s *positive* output, never an error string, or a broken guard reads as a pass. The MediaProjection capture path is done and **verified on an android-36 arm64 emulator** via `scripts/smoke-test-capture.sh` — `policy/ScreenCapture.kt` + `policy/FrameGate.kt` (pure) + `ScreenCaptureService.kt` + `FrameSink.kt`: a `mediaProjection`-typed FGS that mirrors the display into a small `ImageReader`, reduces each frame to a dHash, and passes the few that survive a one-per-second rate cap and a change threshold to a sink. **It captures and gates; it does not classify** — the sink is a counter until `packages/image-sandbox` exists, and nothing is ever written to disk. Recorded in the plan: the start order is consent → `mediaProjection` FGS → `getMediaProjection`, `registerCallback` must precede `createVirtualDisplay` on Android 14+, the consent token is per-session and single-use so **capture cannot be restored after a reboot** (`BootReceiver` deliberately does not try), and `FrameGate` compares against the last frame *analysed* rather than the previous frame so a slow dissolve cannot walk past it. One defect found on device that tests could not see: `onActivityResult` runs before `onResume`, so a running-flag set inside the service made the screen report scanning off over a live projection — it is set before the service is dispatched now. Two things adb cannot drive, both recorded rather than skipped quietly: the disarm-driven stop (flipping the mode needs the process down, which kills the projection first) and revoking `PROJECT_MEDIA` mid-session (the op is checked at grant time only); the revoke path is verified through the system's stop-sharing chip instead. **Next: give the sink something to call — but not `image-sandbox`.** The runtime split decided 2026-07-18 (`docs/decisions/learning-from-feedback.md`) is LiteRT for the frozen backbone on Android, ONNX Runtime for the same job on Windows, and the head in Rust over UniFFI shared by both; image-sandbox is the ONNX half and belongs to `mitm-proxy`. Measured: image-sandbox builds for `aarch64-linux-android` with `--features onnx`, but `ort` has no prebuilt runtime for `x86_64-linux-android` or `armv7-linux-androideabi`, which would cost two of the three ABIs. The mobile image path is therefore three unbuilt pieces — the backbone on LiteRT, `packages/classifier-head` over UniFFI, and model provisioning from `filesDir`, since `data/models/` is gitignored and **no artifact exists on disk**. Planned as §9 and step 13 of `docs/components/mobile/plan.md`, with the crate in `docs/components/classifier-head/plan.md`; the head crate needs no artifact and is where to start |
| `packages/mitm-proxy` | Rust | Plain HTTP forwarding + TLS state/cert generation + CONNECT handler + HTTP/1.1 tunnel loop with phase 3/4/5 scan hooks done; text-policy wired into scan_url/scan_body; **`image-sandbox` now wired into the Phase 4 hook** and verified live — an HTTPS PNG fetched through the tunnel returns `403 Blocked` at `--image-threshold 0` and 200 at `1`. Inference runs under `spawn_blocking` because `image_scanner` is a sync `Fn` called inside the async handler. `cli.rs` adds optional `--listen`, `--ca-dir`, `--image-model`, `--image-threshold`; every default reproduces the old hardcoded values so `ProxySupervisor` needs no change, and without `--image-model` images pass through exactly as before. **Two pre-existing gaps found while verifying, neither introduced here and neither fixed:** (1) `forward::forward_http` — the plain-HTTP path — takes no `ScanHooks` at all, so URL, body *and* image scanning apply only to CONNECT-tunnelled traffic; (2) leaf certs carry no Authority Key Identifier, so OpenSSL-based clients (Python, Node, Linux curl) reject them with `Missing Authority Key Identifier` even though Firefox accepts them. **`ProtectionMode` runtime switching is done** — `Full`/`WarnOnly`/`Off` threaded through `main.rs` as an `Arc<AtomicU8>` (`mode_cell`), read by `url_scanner`/`body_scanner` on every request so a mode change (e.g. a future desktop `config_update` IPC message) takes effect without rebuilding `ScanHooks`. `image_scanner` is not yet mode-gated and still runs unconditionally, since it predates `ProtectionMode` and covering it is unscoped follow-up work |
| `packages/image-sandbox` | Rust | **v0 done: decode → tile → ONNX → max → verdict, wired into `mitm-proxy`.** The deployed geometry is **tile-max**, not the evaluation centre crop: `preprocess_tiles` resizes the shorter side to **224 exactly** (not `224*1.14` — no crop follows), slides 224 windows along the longer axis at a half-window stride with the last flush to the far edge, and `sandbox.rs` takes the **max** score over tiles. Averaging is wrong here — it dilutes a small explicit region into a large safe background, the exact failure being fixed. A near-square image yields **one** tile, so ordinary imagery costs one inference; the 8:1 aspect clamp now bounds *inference count* (worst case 15) rather than allocation. **Both v0 constants were guesses and both were wrong**, now measured (see the `machine-learning` row): threshold 0.20 → **0.4650**, floor 32px → **96px**. A threshold belongs to a model **and** a geometry — the same checkpoint under a centre crop operates at 0.2717, and reusing it under tile-max over-blocks 14.73% against 10.09% with nothing failing to indicate it. `preprocess` (centre crop) is **kept**, because every published figure was measured under it and `tests/parity.rs` pins it against torchvision — that fixture caught a real off-by-one (torchvision's `CenterCrop` rounds the half-pixel offset up; truncating shifted the crop 1px and moved the tensor mean by 0.0085). `tests/tile_parity.rs` does the same for the tiled path against `inputs.tile_geometry`, using a fixture whose green channel ramps left-to-right so tile **position** shows up in each tile's mean — matched torchvision on the first run. `classifier.rs` pins the score contract from `labels.py`/`eval.py` (`softmax(logits)[1]`, block at `>=`) behind a non-default `onnx` feature; `sandbox.rs` fails open on every path — undecodable bytes, size floor, aspect clamp, missing model, inference error — and abandons the whole image if any *one* tile fails, since a max over a subset scores a different image. 50 tests. pHash + SQLite **not built** — no hash database exists or can be populated in-repo, so it is a cache in front of the model rather than a prerequisite. A fully convolutional equivalent (MobileNetV3's pool+linear head → 1×1 convs, one pass whose spatial max equals the tiled max, plus a heatmap for the screen path) is the noted follow-up if 15 passes ever costs too much |
| `packages/net-shield` | Rust | radix domain/IP filter done; SNI parser done; tun adapter + PacketSink dispatch done; NetShield struct + run loop done (Windows Wintun path); smoke-test done — all 5 plan steps complete. **DNS path added for the Android VPN** — `dns.rs` (RFC 1035 question parse + NXDOMAIN synthesis; compression pointers in a QNAME are rejected, never followed), `udp.rs` (IPv4/UDP framing with RFC 1071 checksums; fragments refused), `dns_shield.rs` (`DnsShield::inspect` — one pure `Ignore`/`Blocked`/`Forward` decision per packet). 70 tests. Note the deliberate asymmetry with `NetShield`: `PacketSink::pass_packet` assumes Wintun-style re-injection, which **Android does not offer**, so the DNS path returns bytes for the caller to write instead of dispatching through a sink |
| `packages/net-shield-ffi` | Rust | UniFFI wrapper over net-shield's DNS path — `DnsGuard` + `inspect`/`wrapResponse`, `DnsDecision` as an enum-with-fields so the Kotlin binding is a sealed class. Built by the same `apps/mobile/scripts/build-ffi.sh`, which now loops over both FFI crates. Ships a **placeholder** rule set (one RFC 2606 reserved name), exactly as text-policy-ffi ships a placeholder dictionary; real lists go through `with_blocked_domains` at runtime |
| `native-modules/win-daemon` | C++20 | WinEvent hooks + message loop; no capture/OCR/IPC yet |
| `native-modules/mac-daemon` | Swift / SwiftPM | **Layer 1 (network path) complete and verified end to end on macOS 26.5.** `CommandRunner` edge + `FakeCommandRunner`, `NetworkServices` parsers, `CATrust` (System-keychain root install/trust/remove), `ProxyConfiguration` (per-service snapshot → apply → restore, `DefaultBypass`), `ProxySupervisor` (pure ordering state machine + `RestartBackoff` + executor over `MitmProxyProcess`/`TCPListenerProbe`), `FirefoxTrust` (`ImportEnterpriseRoots` via the `org.mozilla.firefox` preference domain — **kept only for the tamper-model case, not needed for coverage, see below**), and the `holy-blocker-macd` CLI verbs including `run`, `permissions`, `capture`, `bundle`, `bundle-status`, `launchd-plist` and `agent`. 169 tests. Live: CA install idempotent and independently confirmed via `security verify-cert`; a `URLSession` fetch routed through the supervised proxy (`forwarding method=GET host=example.com port=80`, 200) and `openssl s_client -proxy` was served `issuer=CN=Holy Blocker Local CA`; SIGTERM restores all four services byte-identically. Two traps recorded in the plan: **macOS `curl` is SecureTransport-built, ignores `--cacert`, and skips system proxy settings — never verify coverage with it**, and settings must be applied exactly once per run or a restart re-snapshots our own proxy as the "prior" state. **Firefox needed no policy at all**: `security.enterprise_roots.enabled` defaults to `true`, so `CATrust` covers it — and Firefox *deliberately* ignores `ImportEnterpriseRoots` when it is the only policy present (short-circuits to INACTIVE before applying), so any future use of `FirefoxTrust` must ship a companion policy. Read the shipped logic from `omni.ja` (a plain zip), not mozilla-central, which is versions ahead. Browser coverage is now proven: with the CA installed, **Firefox 152 renders an HTTPS page through the proxy** (this required fixing the leaf-certificate validity bug in `packages/mitm-proxy` — see that row). Firefox's own pinned Mozilla endpoints still fail closed, as expected for pinned clients. Also open: `mitm-proxy` has no CLI args so the port is fixed at 8080; `ProxyConfiguration.restore()` is non-atomic per service and an interruption can leave a proxy enabled with an empty server (the persisted snapshot self-heals it on the next `proxy-restore`). **Layer 2 (render path) is fully specified — modules 0 and 7–14 in the plan — and `PermissionGate` (module 7) is the first piece built.** It ships `PermissionSnapshot` over the three non-prompting preflight APIs plus the four zero-permission environment signals, `assess()` (snapshot → `protected`/`weakened`/`blind`), `transitions()` (the reportable tamper events), pure `SystemEnvironment` parsers, a `PermissionProbing` edge with a fake mirroring `CommandRunner`, and a `permissions` CLI verb; the environment signals are confirmed live. Built ahead of module 0 deliberately, since none of it needs a grant — but **a real grant and an observed revocation are still outstanding and blocked on module 0**, because TCC keys grants to a signing identity and `permissions` run from a shell reports the *terminal's* grants. Five implementation findings are recorded in the plan, the load-bearing ones being that **`CGPreflightScreenCaptureAccess` returns a `Bool` and so cannot distinguish denied from never-asked** (not-granted is reported as `undetermined`, the recoverable one — only `IOHIDCheckAccess` reports all three states); **`csrutil status` has a third answer**, `unknown (Custom Configuration)`, parsed as *not* enabled since that is exactly the partially-disabled state being watched for; **`dscl . -list /Users` returns 133 rows** and must be taken with `UniqueID` and filtered to UID ≥ 500 or a bypass account is invisible in the noise; and **`kern.boottime` moves on an NTP correction**, so a five-second tolerance separates a clock adjustment from a reboot. Also: `kAXTrustedCheckOptionPrompt` is an imported mutable global and does not compile under Swift 6 strict concurrency — its literal value is used. `assess()` implements the recommended warn-and-report option (a self-admin user reads `weakened`, never `protected`) **without settling the product decision**, which is still open in `content-interception.md`. **Module 0 (the signed bundle and the `launchd` split) is also done** — `AppBundle` (layout + `Info.plist` + `assemble`), `LaunchdJob`, `CodeSigning`, `scripts/bundle.sh`, and an `agent` entry point that polls `PermissionGate`. Verified live: the bundle passes `codesign --verify` as valid and satisfying its Designated Requirement, and the LaunchAgent bootstraps into `gui/501`, reaches `state = running`, takes a baseline, and boots out cleanly — under launchd, so the responsible process is the agent rather than a terminal. Five findings, the first of which **contradicts the plan it was written from**: (1) **there are no usage-description Info.plist keys for these three capabilities** — the list `tccd` actually reads on macOS 26.5.2 has no `NSScreenCaptureUsageDescription`, no `NSInputMonitoringUsageDescription` and nothing for Accessibility, so `CFBundleName` carries the whole message (extract the real list with `strings .../TCC.framework/Support/tccd | grep UsageDescription`, do not trust blog posts); (2) **a bare SwiftPM binary is already ad-hoc signed** since Apple silicon will not run an unsigned Mach-O, so the problem is not an absent signature but a `cdhash`-derived one that changes every build; (3) `Bundle.main.bundleURL` is the *containing directory* when there is no bundle, so handing it to `codesign` fails with "bundle format unrecognized" in exactly the case a diagnostic is needed; (4) **`codesign -dvv` writes to stderr** — read stdout and every bundle looks unsigned; (5) assembly must delete the old bundle, since a stale `_CodeSignature` makes `codesign` refuse. The split is encoded, not just described: the agent carries `LimitLoadToSessionType = Aqua` and **no** `UserName` (a TCC grant belongs to the logged-in user; root cannot hold one), the daemon carries `UserName = root` and no session limit. **`ScreenCapture` (module 8) is also done** — `CapturedFrame`, `PixelBufferCopy.depad` (fixes the `CVPixelBuffer` row-padding trap), `FrameDeliveryStatus` + `FrameCache` (retains the last `.complete` frame across `.idle`/`.blank`/`.suspended` deliveries so a static image still has something to serve), `FrameAnalysis.isAllBlack` (turns the DRM-black-frame coverage limit into a callable signal), the `ScreenCapturing` edge + `FakeScreenCapture`, and `SCShareableContentCapture` (`SCShareableContent` → `SCContentFilter` excluding our own bundle → pixel-dimensioned `SCStreamConfiguration` → `SCStream`). 20 tests, all on the pure logic; the edge is exercised only through the fake, per the `CommandRunner` pattern. A `capture` CLI verb was added and **confirmed live to reach the real TCC check**: run from a shell with no grant it fails with `SCStreamErrorDomain Code=-3801` rather than silently returning nothing — the same responsible-process gap as `PermissionGate`, so the PNG-by-hand check the plan calls for is blocked on the same outstanding module-0 certificate and a human grant, not on anything in this module. **A stable signing identity now exists** — a self-signed `Holy Blocker Dev` code-signing certificate in the development machine's login keychain, wired via `HOLY_BLOCKER_SIGNING_IDENTITY`, reproducible/rotatable via `scripts/create-dev-signing-identity.sh`; see `docs/components/mac-daemon/signing-identity.md` for the runbook. `bundle-status` now reports `signed(authority: "Holy Blocker Dev")` with `grants survive a rebuild: true`. **Still open and tracked in `docs/components/mac-daemon/backlog.md`:** neither `PermissionGate` nor `ScreenCapture` has seen a real grant or a real revocation yet — that now needs only a human to click the prompt, not a certificate. Specifying it changed the plan in four ways. (1) The old sketch was **missing a scan scheduler and an IPC module**: it had capture/overlay/hooks/AX-text/fullscreen/permissions but nothing to debounce and schedule scans (the Windows `scan_loop`) and nothing to carry verdicts to Electron (the Windows named pipe), so the daemon could have detected but not responded. (2) A **signed `.app` bundle is step zero, not a later step** — TCC keys grants to a code signature, so every grant made to an unsigned SwiftPM binary dies on the next rebuild; and the trap that actually costs a day is the **responsible-process rule**, where a CLI launched from a terminal has its grant attributed to Terminal/iTerm, so it appears to work, covers every other tool run from that shell, and evaporates under `launchd` — **never validate a TCC path from a shell**. (3) **Layer 1 and Layer 2 cannot be one process**: Layer 1 wants root and no session, Layer 2 needs a GUI session (`NSApplication` + `.accessory` policy) and must not be root, so it is a `LaunchDaemon`/`LaunchAgent` split. (4) `PermissionGate` moves **first**, because permission loss is a tamper event to report, not an error to log — the same lesson the Android `GuardStatus` service paid for. Capture traps recorded: `CVPixelBuffer` rows are **padded** (`bytesPerRow ≥ width*4`) and a naive copy shears the frame while still scoring as *something*; `SCStream` is change-driven so a **static image starves it** (`.idle` frames carry no surface — retain the last `.complete` frame, since a still image is exactly what must be caught); `SCStreamConfiguration` defaults to **point, not pixel** dimensions; and **DRM/HDCP content captures as black** by design. Prefer `NSEvent.addGlobalMonitorForEvents` over `CGEventTap` for scroll (a tap is silently disabled on timeout and needs a `CGEventTapEnable` re-arm); Chromium/Electron apps expose an **empty AX tree** unless `AXManualAccessibility` is set. — **measured wrong in module 12 below; the symptom is real, the fix is not**. **The build-environment note is stale in one respect: full Xcode is now installed** (`/Applications/Xcode.app`, Swift 6.3.3, macOS 26.5.2), and ScreenCaptureKit/AppKit/IOKit.hid were verified to import and run. **One open product decision blocks onboarding:** what to do when the protected user is a local admin (which defeats the tamper model on both layers) — the plan recommends warn-and-report over refuse-to-run, and it must be resolved in `docs/decisions/content-interception.md`. Note the development machine **is** in the `admin` group, so it is itself the unprotected configuration. The **macOS tamper model is now analysed in full** in `content-interception.md`, and the conclusions are load-bearing. (a) **A root `LaunchDaemon` is the macOS device-admin equivalent** — Android needed DeviceAdmin only because Android apps can never be root, and Android's *uninstall refusal* is OS-enforced rather than something the app does; macOS has no such API but supplies real privilege separation instead. (b) The structural break is that **macOS users have a terminal and Android users do not**, so guarding the Settings pane guards one door among many (`tccutil reset` was measured to run **without `sudo`**). (c) Therefore the design goal under the admin model is **"no bypass route that is both easy and silent"** — prevention is unreachable, but a route-by-route coverage table is satisfiable **entirely with signals needing zero permissions**: `csrutil status`, `dscl . -list /Users`, `dscl . -read /Groups/admin`, `kern.boottime`, all verified readable ungranted, plus `NSWorkspace.frontmostApplication` (bundle ID free; **window titles are redacted** — 0 of 21 exposed one without Screen Recording). Watch **outcomes, not routes**: one revocation poll catches both the Settings pane and `tccutil`. (d) **Text-layer command interception cannot work** — encoding is non-injective (`base64`/quoting/variable assembly), `eval` makes it halting-shaped, and the keyboard is one modality of many; the sound boundary is Endpoint Security's `AUTH_EXEC`, verified present, whose AUTH messages carry a **`deadline` past which the kernel kills your client**, making hash-keyed decision caching mandatory. (e) **A kext is strictly dominated by ES** — `KAuth` removed, MACF never public, signing cert harder to get than the ES entitlement, needs a local Recovery boot per machine; **zero third-party kexts** are loaded on the dev machine. (f) **SIP-off/`systemextensionsctl developer on` cannot ship**: with SIP off `TCC.db` is directly writable, a bigger hole than the one closed. (g) **Remote partner-held admin works** — Screen Sharing supplies the GUI session that admin prompts need (SSH cannot), so the partner need not be nearby; **FileVault pre-boot is the irreducible wall**, and Secure Token/bootstrap-token escrow gates remotely-created accounts. (h) Automating approval means a **root privileged helper, never a stored password**, and it **cannot grant TCC even as root**; an intent engine must **select among fixed enumerated operations and never generate a shell command**, or it is a confused deputy with root. (i) The standard-user model is **structurally hard for a developer** (GTFOBins `sudoers` escapes), so split machines or the admin model are the real options. **Covenant Eyes is installed on the dev machine** (`com.Cvnt.ce.FirewallExtension`) — the category leader chose the **network** boundary, and it occupies the surface the transparent proxy would want. **One verification blocks any tamper-resistance claim:** whether a *standard* user can `tccutil reset` Screen Recording — measured only from an admin account so far. Also flagged: the mode→action mapping would be its **third** implementation (Rust/C++/Swift) and UniFFI generates Swift bindings natively, so consuming `text-policy-ffi` is the cheap correct answer — undecided because it affects Windows too. **`ScanVerdict` now carries a `regions: [ImageDetection]` field** for bounding-box-level image localization — see `docs/architecture/content-classification.md`'s "Image Localization" section for the two paths to populate it (a real detector model, not yet started in `machine-learning`, or a coarse-grid classifier fallback); nothing populates it yet, `NullScanner` and every test double return an empty list, and `Overlay` still only covers whole screens. **`Scanner` + `ScanLoop` (module 9), and the pure parts of `Overlay` (module 10) and `DaemonIPC` (module 14), are now done** — `ScanLoop.tick(now:)` is pure with respect to time (debounce, image/OCR cadence split, frame-fingerprint dedup, `ProtectionMode` downgrade, all injected-clock testable with no real capture); `OverlayPlan.plan(intent:screens:)` decides one placement per screen (level/collection-behavior recorded as data); `DaemonIPCCodec` does length-prefixed-JSON framing with an incremental decoder (`.needsMoreData`/`.message`/`.oversized`/`.invalid`) and validated `scan_event`/`config_update` types, with no real Unix-socket transport yet and not yet wired into `apps/desktop/src/main/daemon-ipc.ts` (whose current shape doesn't match). **`TextPolicyFFI` (module 16) is done, which settles the mode→action question above: the daemon consumes `packages/text-policy-ffi`'s `PolicyEngine`/`evaluate` over UniFFI rather than adding a third implementation of the thresholding rules** — `scripts/build-ffi.sh` (modelled on `apps/mobile/scripts/build-ffi.sh`) builds `libtext_policy_ffi.dylib` for `aarch64-apple-darwin` and generates Swift bindings into two gitignored targets, `AppBundle` gained `Contents/Frameworks` + a `libraries:` argument + `nestedCode(in:)`, and `CodeSigning.sign` takes `nestedCode:` so the dylib is signed **before** the bundle seals it. Verified live: `codesign --verify --deep --strict` validates the nested dylib and the bundle satisfies its Designated Requirement, and the bundled binary still runs with the build-tree `.ffi/lib` **moved away entirely**, proving the load is bundle-relative. Five findings, two contradicting the plan they were written from: (1) the C target's name is **not a free choice** — the generated Swift contains a literal `import text_policy_ffiFFI` and a SwiftPM `systemLibrary` module name *is* its target name, so `CTextPolicyFFI` was impossible without patching generated code every build; (2) **`DYLD_LIBRARY_PATH` does not work for `swift test`** — SIP strips every `DYLD_*` across the exec of Apple's signed `swiftpm-testing-helper`, so the rpath must be declared on the **`TextPolicyFFI` target** (linker settings propagate to every product linking it, which is the only way to reach the `.xctest` bundle without `unsafeFlags` on the test target); (3) `unsafeFlags` on a **non-test** target does *not* break test discovery — that warning is test-target-specific; (4) **cargo stamps `LC_ID_DYLIB` with the absolute path of its own target directory**, so without an `install_name_tool -id @rpath/...` rewrite the app keeps loading the copy under `packages/text-policy-ffi/target` and fails on any other machine — and that rewrite invalidates cargo's ad-hoc signature, so re-sign immediately; (5) an embedded dylib signed *after* the bundle leaves the seal describing an unsigned library, refused at launch rather than at build (`--deep` would hide this and Apple documents it as not to be used). Note a fresh checkout **cannot build until `scripts/build-ffi.sh` has run** — SwiftPM fails at package-layout resolution with `missing system target module map`, which reads like a broken manifest. **`AccessibilityText` (module 12) is done** — `AXNodeID` (an opaque handle, so the walk needs no AX types), `AXWalkLimits` (40 deep / 2000 nodes), `AXTextWalk` (text plus which bound stopped it), the `AXElementProbing` edge + `FakeAXElementProbe`, a pure iterative walk with `CFEqual`-keyed cycle detection and adjacent-only dedup, `SystemAXProbe` (per-message `AXUIElementSetMessagingTimeout` of 300ms plus a 500ms budget on the whole walk, after which every read returns nothing and the walk unwinds itself), and an `ax-text [delay] [no-manual]` verb that walks **twice** from one process. **The Chromium trap the plan was written around is measured wrong, in the direction that matters**: Chrome 151 **rejects** `AXManualAccessibility` (`kAXErrorAttributeUnsupported`, `-25205`) and `AXEnhancedUserInterface` (`kAXErrorNotImplemented`, `-25208`), while Obsidian and Todoist (Electron) **accept** the write and still expose no `AXWebArea` four seconds later — so the opt-in is refused by the app it was written for and accepted by the ones where it changes nothing. What actually built Chrome's tree was **being an AX client at all**: the first walk returned 65 nodes of toolbar chrome, and a walk ~3s later returned the page's heading, body text, button label and image `alt` text. The write is kept (one ignored message, and the only lever on older Chromium). The consequence is unchanged and is what to design around — **the first walk against a browser is legitimately thin, only a repeated walk sees content**, which session 4's `AccessibilityScanner` gets for free on the OCR cadence. Four smaller findings: a `static let` of type `CFString` **does not compile under Swift 6** (non-`Sendable` global — held as a `String` and bridged at the call site, same shape as `PermissionGate`'s `trustedCheckOptionPrompt`); **cycle detection cannot be replaced by the depth bound**, since a cycle still costs a full 40-level walk on every branch entering it; **a real window can nearly saturate the 2000-node bound** (a Telegram window measured 1524 nodes / 61 KB); and **an app can report zero windows while running** (Safari, with its windows on another Space). Also flagged for session 4: **the newline element separator is not a boundary and no character could be** — `text-policy`'s `collapse_whitespace` rewrites it to a space and `compact` strips it, so a lexicon phrase can match across two unrelated elements, and the only real fix is evaluating elements separately rather than as one blob. Outstanding: Safari's page *body* is not yet confirmed extracted (Safari reads fine, but every attempt to put the test page in front of it lost focus back to the terminal) — a manual re-run of `ax-text`, not a code question. **`Overlay` (module 10) is now complete on both halves** — `OverlayWindow.swift` adds `AppLifecycle.configureAccessoryApp()` (`NSApplication.shared` + `.accessory`, *without* running the loop, so the caller keeps the thread), `ScreenConfiguration.current()`/`init?(screen:)` (a screen's identity is its `CGDirectDisplayID`, readable only through the untyped `"NSScreenNumber"` device-description key — there is no `NSDeviceDescriptionKey` constant for it), the mechanical mappings onto `NSWindow.Level`/`NSWindow.CollectionBehavior`, a `@MainActor OverlayController` owning `[screenID: NSWindow]` and observing `didChangeScreenParametersNotification`, an `OverlayContentView` backdrop, and an `overlay [seconds] [passive]` verb. Three things worth carrying: (1) **the reconcile is pure and tested, not AppKit glue** — `OverlayReconciliation.diff(existing:planned:)` lives in the AppKit-free `Overlay.swift` and splits the live window set into create/update/close, because the scan loop reconciles several times a second and rebuilding an unchanged window would flicker the overlay continuously; pulling it out is what leaves `OverlayWindow.swift` branch-free and so legitimately test-exempt, the same exemption `SCShareableContentCapture` and `SystemAXProbe` take. (2) **The backdrop reads no pixels at all** — `NSVisualEffectView` blurs the composited desktop *behind* the window, matching the proxy's injected `blur(24px)` over `rgba(0,0,0,0.55)` with no captured frame existing to be mishandled, which is stronger than the plan's "the frame is a texture" rule. (3) **A `Timer` scheduled after `NSApplication.run()`, or created off the main run loop, silently never fires** — the same trap as an `AXObserver` with no run-loop source, and the one session 5's agent loop will hit. The verse card and its two-button deliberate choice are deliberately not built (they need the verse table and a dismissal path back to the scan loop); a placeholder label marks the spot. **The visual itself is still unverified** — the process runs, covers the one attached display and exits cleanly, but confirming it is *drawn*, sits over native-fullscreen video, and reconciles across a display connect/disconnect needs a human at a screen; `screencapture` cannot stand in, since a terminal-run binary has no Screen Recording grant (`could not create image from display`). **`AccessibilityScanner` (session 4 of the e2e split) is done — the daemon's first real `Scanner`**, replacing `NullScanner` on the text path: it walks the frontmost window's AX tree (module 12) and scores it with the Rust policy engine (module 16), which **settles module 9's mode→action open question** — Rust picks the `Action`, Swift never re-derives it. `PolicyEngineHandle` is the UniFFI seam (`RealPolicyEngine` wraps the generated `PolicyEngine`, `FakePolicyEngine` ships in the library beside `FakeAXElementProbe`), so `Scanner.swift`/`ScanLoop.swift` and their 62 tests are **untouched** and never see a UniFFI type. Deliberately *not* pushed across the boundary: `ScanLoop.applyProtectionMode` stays plain Swift, because the `full`/`warn`/`off` downgrade is a product setting this daemon owns rather than the scoring rule the crate owns. Four findings. (1) **`scan(_:)` ignores its `frame`** — AX text is not frame-driven, so the scanner does its own read; the flip side, flagged not fixed, is that `ScanLoop.tick` gates on `!frame.isEmpty`, so without a Screen Recording grant the text path never runs even though it touches no pixels. (2) **A rate-limited tick must repeat the last verdict, not allow** — `ScanLoop` calls one `Scanner` on both its ~500ms image and ~1–2s OCR cadences, and returning `.allow` between walks would tear the overlay down and rebuild it several times a second over content that never stopped being blocked; the cached verdict is load-bearing state, not an optimization. (3) **A walk that found nothing still closes the gate**, since a browser with no focused window is the expected case (module 12: a first walk against a browser is legitimately thin), not a reason to retry in 500ms. (4) The 5→3 action mapping is a real narrowing and lives in Swift — **`Blur → .warn`** with its own named test, because no partial-cover visual exists yet; `Log → .allow` since it is report-only and `DaemonIPC` carries `rawAction` anyway. **Module 12's cross-element phrase gap is decided and deferred, not forgotten**: evaluating elements separately would need a wider `AccessibilityText.extract` *and* trades a false-positive class for a false-negative one (text wrapped across two AX elements would stop matching), so it needs a measurement rather than a guess — tracked in `docs/components/mac-daemon/backlog.md`. 308 tests. **Run tests with `scripts/test.sh`, never bare `swift test`** — see `docs/components/mac-daemon/plan.md` |

The packages below are **planned but not yet created** — do not assume they exist:

- `packages/classifier-head` — the classifier head (embedding → score → verdict) in Rust behind UniFFI, plus a later gradient path. Planned in `docs/components/classifier-head/plan.md`. **It exists because the runtime split is per-platform**: LiteRT runs the frozen backbone on Android, ONNX Runtime on Windows, and only the head is shared — see `docs/decisions/learning-from-feedback.md`. Do not wire `apps/mobile` to `packages/image-sandbox`, which is the ONNX half
- `native-modules/win-network` — Windows Service: Wintun driver install, routing rules, named-pipe IPC for net-shield
- `packages/video-watchdog` — async HLS/DASH segment sampler

`native-modules/android-service` was planned but never created — the Android work shipped as `apps/mobile/` instead, and it targets plain Device Admin rather than the Device Owner model the old plan assumed. `docs/components/android-service/plan.md` is a superseded stub pointing at `docs/components/mobile/plan.md`; do not build against it.

Each active package has a step-by-step implementation plan in `docs/components/<package>/plan.md`. Read the relevant plan before starting work on a package — it lists the next modules to add, their types, and the correct implementation order.

Current major areas:

## Development Commands

Use `pnpm` for the JavaScript workspace.

- Install JS dependencies: `pnpm install`
- Run the desktop app: `pnpm dev:desktop`
- Build all JS workspace packages: `pnpm build`
- Typecheck all JS workspace packages: `pnpm typecheck`
- Build the desktop package only: `pnpm --filter @holy-blocker/desktop build`
- Typecheck the desktop package only: `pnpm --filter @holy-blocker/desktop typecheck`

For Rust policy code:

- From `packages/text-policy`, use `cargo test` for tests.
- Use `cargo run` only when validating executable behavior.

For Python ML code:

- The package lives under `machine-learning/src/holy_blocker_ml`.
- Prefer small, importable functions over script-only code so behavior can be unit tested.
- If you add Python tests, place them under `machine-learning/tests` and wire a standard runner such as `pytest` before relying on it.

For the Windows daemon:

- Build with CMake from `native-modules/win-daemon`.
- Keep platform APIs isolated from portable decision logic where practical, so pure behavior can be unit tested separately from Win32 event plumbing.

## Test-First Rule For Logic

For any new business-logic function, write focused unit tests first, then implement the function. This applies especially to:

- classification thresholds and policy decisions;
- text matching, normalization, scoring, or allow/block decisions;
- ML pipeline configuration and artifact-selection logic;
- daemon event filtering, debouncing, IPC message shaping, and state transitions;
- Electron main/preload logic that affects daemon status, local data, or policy decisions.

Frontend-only rendering changes do not need test-first treatment by default, but extracted non-UI logic should still get unit tests.

When a test framework is missing, add the smallest appropriate test setup for the package you are changing instead of leaving new logic untested. Keep tests deterministic and avoid private datasets, explicit sensitive corpora, screenshots, or generated adult-content fixtures in the public repo.

## Code Conventions

- Preserve the existing language boundaries. Do not move daemon, ML, policy, or UI responsibilities into another layer without a clear reason.
- Keep code local-first. Avoid network access in runtime paths unless explicitly requested.
- Prefer pure functions for policy and classification decisions. Put side effects at the edges.
- Keep Electron security settings strict: preserve context isolation and avoid enabling Node integration in the renderer.
- In the renderer, follow the existing React + TypeScript style and use `lucide-react` icons where icons are needed.
- In Rust, keep policy logic in testable modules instead of burying it in `main`.
- In Python, keep training/export orchestration thin and move reusable behavior into importable functions.
- In C++, keep Win32 callback glue small and move decision logic into testable helpers when the daemon grows.

## Specification References

Any code that implements a network protocol, binary wire format, or OS-level interface must be traceable to its authoritative specification. This applies to packet parsers, TLS record handling, IP/TCP header field offsets, IANA registry values, Win32 API contracts, and similar low-level work.

**In code:** every magic number, byte offset, or field layout must have an inline comment citing the document and section it comes from — for example `// RFC 791 §3.1` or `// Wintun API docs — Session::receive_blocking`. Name constants instead of repeating literals, and put the citation on the constant.

**In plan files (`docs/components/<package>/plan.md`):** each module section that touches wire formats or OS interfaces must include a "Reference documents" subsection listing the specs an implementer needs to read. Link to the canonical online version of each document so it can be consulted directly:

- IETF RFCs: `https://www.rfc-editor.org/rfc/rfcNNNN` (the RFC Editor HTML version is easier to navigate than the plain-text original).
- IANA registries: link the specific registry page, not just the top-level site.
- Microsoft Win32/WinRT docs: link the specific `learn.microsoft.com` page for the API or concept.
- Wintun: `https://www.wintun.net` and the repository README at `https://github.com/WireGuard/wintun`.

When in doubt about a field value or offset, consult the linked document rather than inferring from existing code. The web versions of RFCs are searchable and have anchor links per section.

## Documentation

Docs are plain Markdown under `docs/`. Keep them generator-neutral and use relative links between pages. Do not add sensitive blocklists, private datasets, explicit evaluation samples, generated adult-content screenshots, or other private moderation artifacts to documentation.

Update docs when changing architecture, daemon responsibilities, classification flow, evaluation strategy, or public development workflows.

When a planned step in any `docs/components/<package>/plan.md` is completed, mark it done in that file (strike the item through and add **Done.**) and update the corresponding status row in the **Current State** table above. If the user asks to revert a completion marker, remove the strike-through and restore the original wording. This keeps the plans accurate without needing a separate sync pass.

## Branch and Commit Conventions

Branch names follow the pattern `<prefix>/<short-slug>` where the slug is a brief kebab-case description of the work. The fuller description lives in the first commit message. Use these prefixes:

| Prefix | When to use |
|---|---|
| `feat` | new feature or capability |
| `fix` | bug fix |
| `refactor` | restructuring without behaviour change |
| `infra` | build system, CI, tooling, scaffolding |

Examples:
- `feat/net-shield-basic-impl`
- `fix/mitm-proxy-tls-cert-chain`
- `refactor/text-policy-scorer-module`
- `infra/cargo-workspace`

Commit messages follow the same prefix convention with the conventional-commits format:
`<prefix>(<scope>): <imperative summary>`

The body should explain *why* the change was made — the what is visible in the diff. Keep the subject line under 72 characters.

## Working Rhythm — Worktree In, PR Out

Every piece of work starts in its **own git worktree** and ends as an **open pull request**, with no
prompting needed for either. Both halves are the default, not a thing to propose and wait on.

### Start: a separate worktree, branched from the base branch

Do not work in the main checkout, and do not branch from whatever happens to be checked out. Create
a worktree on a new branch off the branch that carries the changes this work depends on:

```
git worktree add .claude/worktrees/<slug> -b <prefix>/<slug> <base>
```

`<base>` is **almost always `master`** (this repository's default branch). Use a different base only
when this work genuinely builds on an unmerged branch — for example a session that needs an earlier
session's commits. **If which base is correct is ambiguous, ask before creating the worktree**; the
cost of asking is one question, and the cost of guessing is a branch that has to be redone.

Run every command from the worktree. Note that the git stash stack is shared across worktrees, so
prefer a temporary WIP commit over `git stash`.

### Finish: commit, push, and open the PR immediately

When the work is complete and the [verification checks](#verification-expectations) pass, go
straight through to a PR in one pass — commit, `git push -u origin <branch>`, `gh pr create` — and
then report what was done. Do not stop after the commit to ask whether to push, and do not stop
after the push to ask whether to open the PR. "Complete" means the whole task is done and checked,
including the plan and status-table updates the [Documentation](#documentation) section requires.

**The one exception:** if the request asked to review, inspect, or hold the change before it goes
out — anything of the shape "let me look first", "don't push yet", "just commit" — stop where asked
and leave the rest to them. An earlier request to hold does not carry over to later, separate work.

If a check fails or something in the work is genuinely blocked, that is not "complete": say so
instead of opening a PR over it.

## Verification Expectations

Before finishing a code change, run the narrowest relevant checks:

- Desktop TypeScript changes: `pnpm --filter @holy-blocker/desktop typecheck`
- Desktop build or bundling changes: `pnpm --filter @holy-blocker/desktop build`
- Rust policy changes: `cargo test` from `packages/text-policy`
- Python logic changes: run the package's unit tests, adding a test command if needed
- Windows daemon changes: build with CMake and run any added unit tests
- macOS daemon changes: `./scripts/test.sh` from `native-modules/mac-daemon`. Do **not** use bare
  `swift test` — under a Command Line Tools toolchain it cannot locate swift-testing, and moving
  the needed flags into `Package.swift` as `unsafeFlags` makes SwiftPM silently run zero tests
  while still exiting 0.

If a relevant check cannot be run, report the reason clearly.
