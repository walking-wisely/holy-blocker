{
  "name": "holy-blocker",
  "version": "0.1.0",
  "private": true,
  "description": "On-device content blocking engine with desktop controls, native daemons, and local ML.",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev:desktop": "pnpm --filter @holy-blocker/desktop dev",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
